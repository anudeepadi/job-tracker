{
  "name": "job-search-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Unified job search platform with AI-powered job tracking and analysis",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "setup": "bash scripts/setup.sh",
    "dev": "bash scripts/start-dev.sh",
    "start": "bash scripts/start-prod.sh",
    "migrate-data": "bash scripts/migrate-data.sh",
    "dev:parallel": "pnpm run --parallel dev",
    "dev:web": "pnpm --filter web dev",
    "dev:agent": "cd services/job-agent && python -m uvicorn app.main:app --reload --port ${PYTHON_PORT:-8342}",
    "build": "pnpm run --parallel build",
    "build:web": "pnpm --filter web build",
    "lint": "pnpm run --parallel lint",
    "test": "pnpm run --parallel test",
    "start:agent": "cd services/job-agent && python -m uvicorn app.main:app --host 0.0.0.0 --port ${PYTHON_PORT:-8000}",
    "orchestrate": "cd orchestration && claude-flow start",
    "clean": "pnpm run --parallel clean && rm -rf node_modules",
    "db:start": "docker-compose up -d postgres",
    "db:stop": "docker-compose down",
    "db:logs": "docker-compose logs -f postgres",
    "db:setup": "bash scripts/setup-docker-db.sh",
    "db:reset": "docker-compose down -v && docker-compose up -d postgres && cd apps/web && pnpm prisma migrate deploy",
    "db:studio": "cd apps/web && pnpm prisma studio"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "keywords": [
    "job-search",
    "ai-agent",
    "job-tracker",
    "monorepo"
  ],
  "author": "",
  "license": "MIT"
}
